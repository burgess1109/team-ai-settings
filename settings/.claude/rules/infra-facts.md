---
paths:
  - "**/*.sql"
  - "**/migration*/**"
  - "**/schema/**"
  - "**/model/**"
  - "**/entity/**"
  - "**/*.tf"
  - "helm/**"
  - "**/k8s/**"
---

> **範本檔案。** 尖括號 `<...>` 全部要替換，用不到的整段刪掉。
> 不知道該填什麼？見 repo 根目錄 `CLAUDE.md` 的「怎麼盤點架構事實」。
>
> **只填「主流答案會出錯」的項目。** 跟社群預設一致的東西不用寫——
> 這份檔案的價值在於糾正 AI 的預設值，不是描述整套架構。

# 架構事實

AI 看不到我們的環境長什麼樣，沒寫下來它會用訓練資料裡**最主流的版本**填空。
以下每一條都是那個預設值會出錯的地方。

## 資料儲存

| 用途 | 線上實際使用 | 宣稱相容於 | 規劃時必須注意 |
|---|---|---|---|
| `<關聯式資料庫>` | `<服務名稱>` | `<MySQL / PostgreSQL>` | **不是真的 `<原版>`。** `<列出實際不支援或行為不同的項目>` |
| `<快取 / 佇列>` | `<服務名稱>` | `<Redis>` | `<例：只提供 HTTP API，不支援 blocking 指令與 pub/sub>` |
| `<搜尋 / 分析>` | `<服務名稱>` | `<Elasticsearch / ClickHouse>` | `<例：官方 client 會拒絕連線，必須改用相容 fork 的 client>` |
| `<物件儲存>` | `<服務名稱>` | `<S3>` | `<例：不支援 storage class 與 bucket ACL>` |

> 「**不是真的 X**」這句必須寫出來。讀到「X 相容」時注意力會壓在「X」上，
> 需要一個明確的否定句去對抗預設值。

### `<資料庫>` 的設計約束

只在有「跟原版不同的硬限制」時才需要這一段。範例寫法：

- **`<不支援的功能，例如外鍵約束>`。** 影響：`<這件事改由誰負責>`
  - `<具體要求 1，例如：級聯刪除必須自己實作>`
  - `<具體要求 2>`
- ORM / migration 工具需要的對應設定：

| 工具 | 做法 |
|---|---|
| `<GORM / Ent / Atlas / Prisma…>` | `<對應的設定或寫法>` |

### Migration 流程

- `<例：走平台的資料庫分支 + deploy request，不是對 prod 直接跑腳本>`
- `<例：單次 schema 變更有欄位異動上限，大型變更要拆成多次>`

## 流量入口

- 一律使用 `<實際採用的方案，含 apiVersion>`
- **不要使用** `<社群較新或較主流、但我們沒採用的方案>`，理由：`<既有專案／監控／policy 都建立在前者上>`
- `<兩者容易混淆的地方，例如資源 kind 同名，要看 apiVersion 才分得出來>`

## 執行環境

- 部署形態：`<常駐服務 / 容器 / Serverless>`
- `<例：回應送出後背景工作會被凍結，非同步任務必須交給佇列，不能開背景 thread>`
- `<例：本地磁碟不保證存在，暫存檔要走物件儲存>`
- `<例：會縮到零，行程內排程不會觸發，定時任務要用外部排程器>`

---

## 維護

- **Owner**：`<負責人>`
- **最後確認**：`<YYYY-MM-DD>`

過期的架構事實比沒有更危險——它會讓人和 AI 都停止查證。
基礎設施變更的收尾步驟要包含「更新這份文件」。
